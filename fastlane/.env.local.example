# Copy this file to `.env.local` and replace the placeholder values below.
# Each key configures Fastlane when running `bundle exec fastlane ... --env local`.

# Flutter flavor name used by the Fastfile.
FLAVOR=local
# Entry point Dart file that matches the flavor.
DART_ENTRY=lib/main_local.dart
# iOS bundle identifier for the local flavor (must match Xcode target).
BUNDLE_ID=REPLACE_WITH_LOCAL_IOS_BUNDLE_ID
# Android applicationId for the local flavor (must match Gradle product flavor).
ANDROID_APP_ID=REPLACE_WITH_LOCAL_ANDROID_APPLICATION_ID
# Xcode scheme that Fastlane should build.
IOS_SCHEME=local
# Xcode build configuration corresponding to the scheme.
IOS_CONFIGURATION=Debug-local
# Export method used by gym (development, ad-hoc, app-store, etc.).
BUILD_EXPORT_METHOD=development
# Optional: add Apple IDs, API tokens, or other secrets as needed; keep this file out of version control.
