# Copy this file to `.env.prod` and replace the placeholder values below.
# Each key configures Fastlane when running `bundle exec fastlane ... --env prod`.

# Flutter flavor name used by the Fastfile.
FLAVOR=prod
# Entry point Dart file that matches the flavor.
DART_ENTRY=lib/main_prod.dart
# iOS bundle identifier for the production flavor (must match App Store Connect).
BUNDLE_ID=REPLACE_WITH_PROD_IOS_BUNDLE_ID
# Android applicationId for the production flavor (must match Gradle product flavor).
ANDROID_APP_ID=REPLACE_WITH_PROD_ANDROID_APPLICATION_ID
# Xcode scheme that Fastlane should build.
IOS_SCHEME=prod
# Xcode build configuration corresponding to the scheme.
IOS_CONFIGURATION=Release-prod
# Export method used by gym (development, ad-hoc, app-store, etc.).
BUILD_EXPORT_METHOD=app-store
# Optional: add Apple IDs, API tokens, or other secrets as needed; keep this file out of version control.
