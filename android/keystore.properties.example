# Copy this file to `android/keystore.properties` and fill in your signing
# credentials. The real file is gitignored so it stays local-only.
storeFile=app/upload-keystore.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=upload
keyPassword=YOUR_KEY_PASSWORD
